# DEMO MODE — no Supabase project is required right now. See
# docs/DEVELOPMENT_PLAN.md for the note on this and lib/demo/store.ts for
# the hardcoded admin/labour/truck data.

# Signing secret for session cookies (admin + labour). Falls back to an
# insecure dev default if unset outside production — set this explicitly
# before deploying anywhere real.
SESSION_SECRET=

NEXT_PUBLIC_APP_URL=http://localhost:3000
